using System;
/*
 * namespace is used to organize and group related classes, interfaces, structs, and enums together. It helps avoid naming conflicts and provides a way to logically structure code. In C#, namespaces are declared using the 'namespace' keyword followed by the namespace name. Classes and other types can be defined within a namespace block.
 * 
 * preventing naming conflicts
 * code organization
 */
//namespace MyNamespace; // this is for whole file

namespace MyNamespace2
{
    public class  OrganizedClass
    {
        public string Name { get; set; }
    }
}